{
    "version": 2,
    "builds": [
        {
            "src": "./server/dist/server.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "(.*)",
            "dest": "/server/dist/server.ts"
        }
    ]
}